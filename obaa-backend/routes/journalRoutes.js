const express = require('express');
const router = express.Router();

// Placeholder for journal routes
router.get('/', (req, res) => {
  res.json({
    success: true,
    message: 'Journal routes coming soon'
  });
});

module.exports = router; 